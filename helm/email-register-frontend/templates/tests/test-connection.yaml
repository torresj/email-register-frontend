apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "email-register-frontend.fullname" . }}-test-connection"
  labels:
    {{- include "email-register-frontend.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "email-register-frontend.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

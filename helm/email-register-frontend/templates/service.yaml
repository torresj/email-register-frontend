apiVersion: v1
kind: Service
metadata:
  name: {{ include "email-register-frontend.fullname" . }}
  labels:
    {{- include "email-register-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "email-register-frontend.selectorLabels" . | nindent 4 }}
